# Laptop install script

This is a poor man's script to configure a few laptops.

Review the [install.sh](./install.sh) script and execute, as root, it with:
```sh
wget -O - https://raw.githubusercontent.com/epfl-fsd/config-laptop-stagiaire/main/install.sh | bash
```
